{
  "name": "ssm-lambda-env",
  "version": "0.0.1",
  "description": "Sets up environment for AWS Lambda using SSM, reinitalizes when vars change",
  "main": "lib/index.js",
  "author": "Paul Esson",
  "license": "MIT",
  "scripts": {
    "test": "blueflag-test test",
    "prepublish": "yarn build",
    "build": "rm -rf lib && babel src --out-dir lib --copy-files"
  },
  "private": false,
  "devDependencies": {
    "@blueflag/proto-blueflag-test": "git+ssh://git@github.com/blueflag/proto-blueflag-test.git#^0.11.0",
    "ava": "^0.25.0",
    "babel-preset-blueflag": "^0.6.0",
    "nyc": "^11.4.1",
    "proxyquire": "^1.8.0"
  },
  "dependencies": {
    "aws-sdk": "^2.188.0",
    "rxjs": "^5.5.6"
  }
}
